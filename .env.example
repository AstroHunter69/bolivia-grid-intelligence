# Optional local configuration.
# The current CNDC endpoints used by this project are public and do not require API keys.

MODEL_PATH=models/bolivia_demand_model.pkl
FEATURES_PATH=models/bolivia_demand_features.pkl
CNDC_CACHE_DIR=data/cache
